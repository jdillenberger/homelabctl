name: paperless-ngx
description: Document management system with OCR
category: productivity
version: "2.20.9"
ports:
  - host: 8000
    container: 8000
    protocol: tcp
    description: Web UI
    value_name: web_port
values:
  - name: web_port
    description: Web UI port
    default: "8000"
  - name: secret_key
    description: Django secret key
    secret: true
    auto_gen: password
  - name: db_password
    description: PostgreSQL password
    secret: true
    auto_gen: password
  - name: admin_user
    description: Admin username
    default: "admin"
  - name: admin_password
    description: Admin password
    secret: true
    auto_gen: password
  - name: timezone
    description: Timezone
    default: "UTC"
dependencies:
  - docker
volumes:
  - name: data
    container: /usr/src/paperless/data
    description: Paperless data
  - name: media
    container: /usr/src/paperless/media
    description: Paperless media/documents
  - name: consume
    container: /usr/src/paperless/consume
    description: Paperless consume directory
  - name: export
    container: /usr/src/paperless/export
    description: Paperless export directory
  - name: db
    container: /var/lib/postgresql/data
    description: PostgreSQL data
  - name: redis
    container: /data
    description: Redis data
health_check:
  url: "http://localhost:{{.web_port}}"
  interval: 60s
backup:
  paths:
    - data
    - media
    - db
  pre_hook: "docker exec paperless-ngx-db pg_dumpall -U paperless > /tmp/paperless-db-backup.sql"
  post_hook: "rm -f /tmp/paperless-db-backup.sql"
requirements:
  min_ram: 1G
  min_disk: 10G
  arch: [amd64, arm64]
